<form method="post" action="{{ path('app_besoin_delete', {'id': besoin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ besoin.id) }}">
    <button class="btn">Delete</button>
</form>
